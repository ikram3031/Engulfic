# Antigravity Backend Change Logs (AB01 - AB40)

This document tracks changes made to the `backend` project by Antigravity (AB). Entries are sorted by latest at the top.

---

### AB07: Optional Order Coupon Validation Fix
* **Date**: 2026-08-05
* **Impact**: Backend (OrdersController) & Frontend Store (`useAppStore.js`)
* **Description**: Resolved an issue where orders without coupons failed due to string coercion converting `null` coupon codes to `"NULL"`. Added strict type-checking so empty/null coupon codes bypass backend validation, and updated checkout payload to only include `couponCode` when an active coupon is applied.
* **Changes**:
  - Modified [`OrdersController.js`](file:///E:/AAAAA/backend/src/core/controllers/OrdersController.js) to bypass coupon validation if `couponCode` is `null`, `undefined`, or empty string.
  - Modified [`useAppStore.js`](file:///E:/AAAAA/frontend/src/core/store/useAppStore.js) to set `couponCode` to `undefined` when no coupon is applied.



### AB06: Multi-Client Architecture & Core Code Restructuring
* **Date**: 2026-08-04
* **Impact**: Backend & Frontend (Configuration architecture, branch merge shielding, and directory organization)
* **Description**: Formulated and executed the Multi-Client Git Architecture guidelines. Relocated all backend core module directories (controllers, routes, models, middlewares, utils, helper) into `backend/src/core/` to segregate them from local client modifications. Created dynamic config loader pipelines in both frontend and backend (`config.core.json` + `config.client.json` override pattern) and shielded client configuration files from git merges using `.gitattributes` (`merge=ours`) and `.gitignore`. Consolidated backend API routes under `src/core/routesIndex.js` and updated imports globally.
* **Changes**:
  * Set up standard `.env.example` configurations in `backend` and `dashboard`.
  * Configured git merge settings via `.gitattributes` and `.gitignore` in `frontend` and `backend`.
  * Moved backend files to core directories under `backend/src/core/`.
  * Consolidated backend routing to [`routesIndex.js`](file:///c:/Users/mdikr/Documents/Ecomm%20Full/backend/src/core/routesIndex.js) and simplified [`app.js`](file:///c:/Users/mdikr/Documents/Ecomm%20Full/backend/src/app.js) integrations.

### AB05: Refactor Invoice Email Download Button Layout & Inline Style
* **Date**: 2026-08-04
* **Impact**: Backend (Invoice Email Template Utility)
* **Description**: Custom styled the HTML template's invoice action link with inline CSS to prevent email client stripping. Changed the button copy to "See & Download Invoice" to guide customers clearly.
* **Changes**:
  * Updated design markup inside [`invoiceEmailTemplate.js`](file:///e:/AAAAA/backend/src/utils/invoiceEmailTemplate.js).

### AB04: Update Default Email Templates logoUrl Fallback to server.decantrebd.com
* **Date**: 2026-08-04
* **Impact**: Backend (Email templates route fallback)
* **Description**: Updated the fallback `logoUrl` parameter inside the email route to load the correct logo file from server uploads directory instead of external placeholder pages.
* **Changes**:
  * Modified default URL in [`EmailRoute.js`](file:///e:/AAAAA/backend/src/routes/EmailRoute.js) to point to `https://server.decantrebd.com/uploads/logo.webp`.

### AB03: Update Controller Payloads for createdBy & updatedBy, Fix OTP Logo & Footer
* **Date**: 2026-08-04
* **Impact**: Backend (All CRUD controllers, SMTP templates, and delivery)
* **Description**: Updated Brand, Category, Coupon, Product, and Member controllers to support explicit `createdBy` and `updatedBy` fields from request body payloads. Configured Nodemailer to send the Decantre logo as an inline Content-ID (CID) attachment using a locally downloaded asset, changed SMTP display name to "Decantre BD", and cleaned up email footer text.
* **Changes**:
  * Added `createdBy` and `updatedBy` fallback checks from req.body across [`BrandController.js`](file:///e:/AAAAA/backend/src/controllers/BrandController.js), [`CategoryController.js`](file:///e:/AAAAA/backend/src/controllers/CategoryController.js), [`CouponController.js`](file:///e:/AAAAA/backend/src/controllers/CouponController.js), [`ProductsController.js`](file:///e:/AAAAA/backend/src/controllers/ProductsController.js), and [`MembersController.js`](file:///e:/AAAAA/backend/src/controllers/MembersController.js).
  * Downloaded logo to local assets at [`logo.webp`](file:///e:/AAAAA/backend/src/assets/logo.webp).
  * Refactored [`otpDelivery.js`](file:///e:/AAAAA/backend/src/utils/otpDelivery.js) to attach the logo via CID and set display sender address format.
  * Modified [`otpEmailTemplate.js`](file:///e:/AAAAA/backend/src/templates/otpEmailTemplate.js) to reference `cid:logo` and remove unnecessary footer text.

### AB02: Fix Nodemailer Module Initialization Race & Validate Delivery Results
* **Date**: 2026-08-04
* **Impact**: Backend (SMTP email utilities & Members Controller error states)
* **Description**: Fixed an issue where the Nodemailer transport was instantiated before environment configuration variables were loaded, leading to `Missing credentials for "PLAIN"` authentication failures. Added dynamic lazy transport creation and enforced validation checks for `sendOtpEmail` results.
* **Changes**:
  * Implemented lazy initialization inside [`otpDelivery.js`](file:///e:/AAAAA/backend/src/utils/otpDelivery.js).
  * Replaced try-catch logs with active `emailResult.delivered` checks in [`MembersController.js`](file:///e:/AAAAA/backend/src/controllers/MembersController.js) (affecting `checkMemberEmail`, `loginMember`, `resendMemberOtp`, and `forgotPassword`).

### AB01: Add checkMemberEmail Endpoint for Member Verification Flow
* **Date**: 2026-08-04
* **Impact**: Backend (Members API Controller & Routes)
* **Description**: Added a new endpoint to query if a member's email exists and is verified. If the email is unverified (such as migrated WordPress users), it triggers a 6-digit OTP code creation and dispatches it via SMTP email.
* **Changes**:
  * Added `checkMemberEmail` function in [`MembersController.js`](file:///e:/AAAAA/backend/src/controllers/MembersController.js).
  * Registered `POST /api/v1/members/check-email` route in [`MembersRoute.js`](file:///e:/AAAAA/backend/src/routes/MembersRoute.js).
